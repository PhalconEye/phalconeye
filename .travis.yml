language: php

php:
  - 7.0.11

script:
  - ant check
  - strace -e open,access ./data/checkstyle/phpcs/bin/phpcs.phar --extensions=php --ignore=build,core/templates,data,config,public,vendor --standard=./data/checkstyle/phpcs/PhalconEye ./core

notifications:
  email:
    - lantian.ivan@gmail.com