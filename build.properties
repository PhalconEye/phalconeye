project.name 			= PhalconEye
app.dir					= app
core.dir				= core
config.dir				= config
data.dir			    = data
public.dir			    = public
build.dir 				= build
dist.dir				= dist
build.src.dir 			= ${build.dir}/src
report.dir				= ${build.dir}/reports
doc.dir					= ${build.dir}/doc
check.dir               = ${build.dir}/src
checkstyle-rules.dir	= data/checkstyle
ignore.dir	            = core/engine/Package/Structure,data,config,public